{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "dumb-init sh -c 'echo \"Starting Outline...\" && yarn db:migrate && echo \"Database migrated, starting server...\" && node build/server/index.js --services=cron,collaboration,websockets,admin,web,worker'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "volumes": [
    {
      "name": "outline-data",
      "mountPath": "/var/lib/outline/data"
    }
  ]
}